{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "pip install",
      "command": "pip install .",
      "dependsOn": []
    },
    {
      "type": "shell",
      "label": "pip install test requirements",
      "command": "pip install -r requirements_test.txt",
      "dependsOn": ["pip install"]
    },
    {
      "type": "shell",
      "label": "pytest",
      "command": "pytest --cov=systembridgemodels --cov-report term-missing -vv",
      "dependsOn": ["pip install test requirements"]
    }
  ]
}
